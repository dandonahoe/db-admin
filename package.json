{
  "name": "db-admin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:web": "pnpm --prefix src/web run start",
    "dev:api": "pnpm --prefix src/api run start",
    "build:api": "IS_BUILDING=true pnpm --prefix src/api run build",
    "build:web": "IS_BUILDING=true pnpm run build"


  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "12.1.0"
  },
  "devDependencies": {
    "eslint": "~8.57.0"
  }
}
